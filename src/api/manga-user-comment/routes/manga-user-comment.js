'use strict';

/**
 * manga-user-comment router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::manga-user-comment.manga-user-comment');
