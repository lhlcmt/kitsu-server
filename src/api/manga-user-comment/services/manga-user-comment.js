'use strict';

/**
 * manga-user-comment service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::manga-user-comment.manga-user-comment');
